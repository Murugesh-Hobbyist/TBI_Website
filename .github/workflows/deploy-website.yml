name: Hostinger Deployment Notice

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deployment-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Native deployment status
        run: echo "Hostinger native GitHub auto-deployment handles pushes to main."
